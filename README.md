# Autonomous

Autonomous is a Flue agent project that runs headless GitHub automations for this repository, using thin TypeScript workflows plus Markdown skills and `bot.config.yml` to resolve issues into pull requests and review pull requests according to the configured rules. For local development, install dependencies with `npm ci` and validate the project with `npx flue build --target node`.
